---
// src/components/ui/MusicPlayer.astro
---

<button 
  id="play-btn" 
  style="
    position: fixed;
    bottom: 1rem;
    right: 1rem;
    z-index: 50;
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 50%;
    border: none;
    background: transparent;
    cursor: pointer;
    font-size: 1.2rem;
  "
>
  🎵
</button>

<script>
  const audio = new Audio('/music/小美满.mp3');
  
  // 页面加载自动播放
  window.onload = () => {
    // audio.play();
    audio.pause();
  };
  
  // 点击暂停/播放
  document.getElementById('play-btn').onclick = () => {
    if (audio.paused) {
      audio.play();
    } else {
      audio.pause();
    }
  };
</script>